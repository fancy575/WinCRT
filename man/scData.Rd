% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scData.R
\docType{data}
\name{scData}
\alias{scData}
\title{Composite-endpoint CRT dataset: scData}
\format{
A data frame with columns:
\describe{
\item{id}{Subject identifier (integer).}
\item{cluster}{Cluster identifier (integer/factor).}
\item{Nc}{Cluster size (number of subjects in the subject’s cluster).}
\item{time}{Follow-up time associated with the row (numeric).}
\item{z}{Treatment arm indicator (typically two levels, e.g., 0 = control, 1 = treatment).}
\item{event}{event type for composite endpoint: \code{0} = censoring; \code{1} = hospitalization;
\code{2} = death.}
}
}
\usage{
data(scData)
}
\description{
A demonstration dataset for win–loss–tie analyses in cluster-randomized trials
with a hierarchical (composite) endpoint. The data are in \strong{long format}:
each subject (\code{id}) may have multiple rows for different event tiers.
}
\details{
Use this dataset to illustrate hierarchical comparisons with \code{\link{wincrt}}
and \code{\link{summary.WinCRT}}.
}
\examples{
data(scData)
head(scData)
}
\keyword{datasets}
